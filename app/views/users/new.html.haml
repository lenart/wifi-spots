- title "Registracija uporabnika"

%h1 Registracija uporabnika

= error_messages_for :user
- if @user.errors.any? do
  %p
    - @user.errors.full_messages.each do |msg|
      = msg
      %br

= form_for @user do |f|

  = render :partial => 'form', :locals => { :f => f }

  %p= submit_tag 'Sign up'